<%= form_for(@work_piece) do |f| %>
  <% if @work_piece.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_piece.errors.count, "error") %> prohibited this work_piece from being saved:</h2>

      <ul>
      <% @work_piece.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :section_id %><br />
    <%= collection_select(:work_piece, :section_id, Section.all, :id, :name, {:include_blank => 'Select a section...'}, {:class =>'customize_me'}) %>
  </div>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, {:placeholder=>'Enter a name'} %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, {:placeholder=>'Enter a title'} %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, {:placeholder=>'Describe your work!'}%>
  </div>

  <div class="form_actions">
    <%= f.submit %>
  </div>
<% end %>
